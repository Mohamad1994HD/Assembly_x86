Project 1:

Interfacing DC motor with Desktop PC via parallel port
where user will use Keyboard buttons ('u','d') mouse buttons (left,right)
to control the speed & rotation of the motor respectively, in addition to 
show the speed value on a 7-segment display.


Project 2:
Implementing oscilloscope via parallel port, using assembly language